{
  "name": "mockoon-data",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "@mockoon/serverless": "^0.1.0"
  },
  "devDependencies": {
    "eslint": "^8.32.0",
    "eslint-config-prettier": "^8.6.0",
    "prettier": "^2.8.3",
    "serverless-domain-manager": "^6.2.1",
    "serverless-offline": "^12.0.4"
  },
  "scripts": {
    "lint": "npx eslint . && npx prettier --write .",
    "check-lint": "npx eslint . && npx prettier --check .",
    "validate": "node validator",
    "offline": "node validator && serverless offline"
  }
}
